# 事件驱动 (Event Driven)

## 基本信息

| 字段 | 值 |
|------|-----|
| 策略 ID | `event_driven` |
| 名称 | 事件驱动 |
| 分类 | 框架 (framework) |
| 优先级 | 45 |
| 默认激活 | ❌ |
| 关联理念 | 第 3 条（筹码效率）、第 5 条（风险排查） |
| 市场状态 | `sector_hot`, `volatile` |
| 别名 | 事件驱动、催化、催化事件 |

## 核心逻辑

围绕业绩、政策、并购、订单、产品发布等事件，评估催化强度、兑现概率和风险边界。

### 判断链

1. **事件分类**
   - 调用 `search_stock_news` 梳理近期关键事件
   - 将事件分为五类：

| 事件类型 | 示例 | 影响路径 |
|----------|------|----------|
| 业绩类 | 财报、业绩预告、快报 | 直接改变盈利预期 |
| 政策类 | 产业政策、监管变化、税收 | 改变行业空间或竞争格局 |
| 订单/产品类 | 重大订单、新品发布、FDA 批准 | 验证商业模式和增长 |
| 资本运作类 | 并购、定增、回购、分红 | 改变资本结构或股东回报 |
| 监管/风险类 | 处罚、诉讼、问询、解禁 | 负面冲击，风险优先 |

2. **影响路径**
   - 判断事件影响的是收入、利润率、估值、融资能力还是仅影响情绪
   - 对重大订单或政策利好，要说明兑现周期和不确定性
   - 对监管、减持、处罚、诉讼等事件，风险优先

3. **市场反应**
   - 调用 `get_realtime_quote` 和 `analyze_trend` 判断事件是否已被价格充分反映
   - 放量上涨但未过关键阻力，可等待确认
   - 高位放量滞涨或利好后冲高回落，应警惕兑现压力

4. **交易计划**
   - 事件未兑现前：强调仓位控制和时间窗口
   - 事件兑现后：重新评估是否从"预期交易"切换为"业绩验证"
   - 对负面事件：先看风险释放是否充分，再考虑反弹

### 评分调整

| 条件 | 调整 |
|------|------|
| 高可信正向事件且价格尚未充分反映 | `sentiment_score +14` |
| 正向事件已大幅兑现 | `sentiment_score -6` |
| 负面事件仍在发酵 | `sentiment_score -15` |
| 事件影响不清晰或信息冲突 | 维持中性并降低置信度 |

## 技术指标与数据依赖

| 数据/工具 | 用途 |
|-----------|------|
| `search_stock_news` | 事件识别与分类 |
| `get_realtime_quote` | 事件前后价格/量能变化 |
| `analyze_trend` | 趋势确认、支撑阻力 |

## 适用场景

- **适合**：财报季、政策窗口期、公司重大事项公告时
- **不适合**：事件真空期、无明确催化时
- **市场阶段**：事件密集期效果突出；平淡期结合其他策略

## 与上下游策略的关系

| 策略 | 关系 |
|------|------|
| `expectation_repricing` | 延伸：事件驱动关注事件本身，预期重估关注市场如何定价 |
| `hot_theme` | 配合：题材热点往往由具体事件驱动 |
| `pead_drift` | 子集：PEAD 是事件驱动在财报场景的特化版本 |
| `growth_quality` | 验证：财报是验证/证伪成长判断的关键事件 |

## 在管道中的角色

- **specialist 模式**：当市场状态为 `sector_hot` 或 `volatile` 时可选
- **时效性强**：事件驱动策略的置信度随时间快速衰减，需关注时间窗口
- **操作建议必须包含失效条件**：如公告不及预期、跌破关键支撑、热度消退

## YAML 配置原文

```yaml
# Event Driven Strategy / 事件驱动

name: event_driven
display_name: 事件驱动
description: 围绕业绩、政策、并购、订单、产品发布等事件，评估催化强度、兑现概率和风险边界。
category: framework
core_rules: [3, 5]
required_tools:
  - search_stock_news
  - get_realtime_quote
  - analyze_trend
aliases: [事件驱动, 催化, 催化事件]
default_priority: 45
market_regimes: [sector_hot, volatile]

instructions: |
  **事件驱动策略（Event Driven Strategy）**

  适用场景：
  - 公司或行业出现明确事件催化，如业绩预告、订单中标、并购重组、政策落地、产品发布、监管处罚、诉讼等。
  - 需要判断事件是短期交易催化、长期基本面改善，还是利好兑现。

  分析框架：

  1. **事件分类**
     - 使用 `search_stock_news` 梳理近期关键事件。
     - 将事件分为：业绩类、政策类、订单/产品类、资本运作类、监管/风险类。
     - 明确事件发生时间，过期或时间未知的信息不能作为主要依据。

  2. **影响路径**
     - 判断事件影响的是收入、利润率、估值、融资能力、市场份额，还是仅影响情绪。
     - 对重大订单或政策利好，要说明兑现周期和不确定性。
     - 对监管、减持、处罚、诉讼等事件，风险优先。

  3. **市场反应**
     - 使用 `get_realtime_quote` 和 `analyze_trend` 判断事件是否已被价格充分反映。
     - 放量上涨但未过关键阻力，可等待确认。
     - 高位放量滞涨或利好后冲高回落，应警惕兑现压力。

  4. **交易计划**
     - 事件未兑现前，强调仓位控制和时间窗口。
     - 事件兑现后，重新评估是否从"预期交易"切换为"业绩验证"。
     - 对负面事件，先看风险释放是否充分，再考虑反弹。

  输出要求：
  - 明确事件性质：利好 / 利空 / 中性 / 不确定。
  - 给出事件可信度、兑现周期、已反映程度。
  - 操作建议必须包含失效条件，如公告不及预期、跌破关键支撑或事件热度消退。

  评分调整建议：
  - 高可信正向事件且价格尚未充分反映：`sentiment_score +14`
  - 正向事件已大幅兑现：`sentiment_score -6`
  - 负面事件仍在发酵：`sentiment_score -15`
  - 事件影响不清晰或信息冲突：维持中性并降低置信度
```

## 参见

- [预期重估 (expectation_repricing)](expectation_repricing.md) — 市场如何定价事件
- [盈利公告漂移 (pead_drift)](pead_drift.md) — 财报事件的特化版本
- [热点题材 (hot_theme)](hot_theme.md) — 题材中的事件链分析
- [成长质量 (growth_quality)](growth_quality.md) — 成长判断的事件验证
