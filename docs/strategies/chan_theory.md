# 缠论 (Chan Theory)

## 基本信息

| 字段 | 值 |
|------|-----|
| 策略 ID | `chan_theory` |
| 名称 | 缠论 |
| 分类 | 框架 (framework) |
| 优先级 | 70 |
| 默认激活 | ❌ |
| 关联理念 | 第 1 条（严进不追高）、第 2 条（趋势交易）、第 3 条（筹码效率）、第 4 条（买点偏好） |
| 市场状态 | `volatile` |
| 别名 | 缠论、缠论分析 |

## 核心逻辑

基于缠论（禅论）的笔、线段、中枢结构，判断趋势级别、买卖点与背驰信号。

**核心框架**：分型 → 笔 → 线段 → 中枢 → 趋势

### 判断链

#### 1. 判断价格结构（中枢识别）
- 调用 `get_daily_history` 获取近 60 日日线数据
- 识别近期价格的高低点序列，判断当前是否在：
  - **震荡中枢**（1 个以上中枢）—— 价格在此区间反复震荡
  - **趋势段**（脱离中枢向上/向下）
- 中枢定义：连续 3 段走势重叠区间
- 趋势定义：连续 3 个同级别中枢均向同一方向移动

#### 2. 背驰判断（最高优先级信号）

| 类型 | 信号 | 策略 |
|------|------|------|
| 顶背驰 | 价格创新高但 MACD 红柱面积缩小 | 卖出/减仓 |
| 底背驰 | 价格创新低但 MACD 绿柱面积缩小 | 买入/加仓 |

- 调用 `analyze_trend` 获取 MACD 数据，与价格高低点对比

#### 3. 买卖点判定

| 类型 | 含义 | 强度 |
|------|------|------|
| **一买**（最强） | 下跌趋势中，最后一个中枢出现底背驰 | 最强买入信号 |
| **二买** | 离开下跌中枢后的第一次回调不破中枢高点 | 次强 |
| **三买** | 中枢震荡后向上突破（不回中枢内） | 追强信号 |
| 一卖/二卖/三卖 | 对称结构，方向相反 | 卖出 |

#### 4. 级别与仓位

| 级别 | 仓位 |
|------|------|
| 日线级别买卖点 | 30-50% |
| 周线级别买卖点 | 50-80% |
| 多级别共振 | 信号最强 |

### 评分调整

| 条件 | 调整 |
|------|------|
| 底背驰 + 一买信号 | `sentiment_score +15` |
| 二买/三买共振 | `sentiment_score +10` |
| 中枢震荡无明确方向 | 维持基准 |
| 顶背驰 / 趋势向下 | `sentiment_score -15` |

## 技术指标与数据依赖

| 数据/工具 | 用途 |
|-----------|------|
| `get_daily_history` | 60 日日线数据，高低点序列 |
| `analyze_trend` | MACD 数据、趋势评分 |
| `get_realtime_quote` | 当前价格、盘中走势 |
| MACD | 背驰确认的核心指标 |

## 适用场景

- **适合**：有明显结构特征的股票、趋势转折点判断
- **不适合**：无结构特征的随机波动、流动性差的品种
- **市场阶段**：趋势转折期或结构清晰的中枢震荡行情

## 与上下游策略的关系

| 策略 | 关系 |
|------|------|
| `wave_theory` | 互补：缠论的结构分析与波浪理论互为补充 |
| `bull_trend` | 验证：缠论买卖点可验证趋势策略的信号 |
| `box_oscillation` | 延伸：中枢震荡与箱体震荡有相似之处 |
| `emotion_cycle` | 配合：背驰往往伴随情绪极端 |

## 在管道中的角色

- **specialist 模式**：当市场状态为 `volatile` 时可选
- **结构性视角**：提供独特的"结构+背驰"分析框架，与其他策略的技术指标视角互补
- **高级知识依赖**：缠论对 LLM 的分析能力要求较高，输出置信度需标注

## YAML 配置原文

```yaml
# Chan Theory Strategy / 缠论
# Based on Zen channel theory: pen, stroke, segment, hub structure analysis.

name: chan_theory
display_name: 缠论
description: 基于缠论笔、线段、中枢结构，判断趋势级别、买卖点与背驰信号。
category: framework
core_rules: [1, 2, 3, 4]
required_tools:
  - get_daily_history
  - analyze_trend
  - get_realtime_quote
aliases: [缠论, 缠论分析]
default_priority: 70
market_regimes: [volatile]

instructions: |
  **缠论（Chan Theory / Zen Channel Theory）**

  核心框架：分型 → 笔 → 线段 → 中枢 → 趋势

  ## 分析步骤

  ### 1. 判断价格结构（中枢识别）
  - 使用 `get_daily_history` 获取近 60 日日线数据。
  - 识别近期价格的高低点序列，判断当前是否在
    **震荡中枢**（1个以上中枢）还是**趋势段**（脱离中枢向上/向下）。
  - 中枢：连续3段走势重叠区间，价格在此区间反复震荡。
  - 趋势：连续3个同级别中枢均向同一方向移动。

  ### 2. 背驰判断（最高优先级信号）
  - **顶背驰**：价格创新高但MACD红柱面积缩小 → 卖出/减仓信号。
  - **底背驰**：价格创新低但MACD绿柱面积缩小 → 买入/加仓信号。
  - 使用 `analyze_trend` 获取 MACD 数据，与价格高低点对比。

  ### 3. 买卖点判定
  - **一买**（最强）：下跌趋势中，最后一个中枢出现底背驰。
  - **二买**：离开下跌中枢后的第一次回调不破中枢高点。
  - **三买**：中枢震荡后向上突破（不回中枢内）。
  - **一卖/二卖/三卖**：对称结构，方向相反。
  - 当前价格所处的买卖点级别决定仓位大小。

  ### 4. 级别与仓位
  - 日线级别买卖点可用较重仓位 (30-50%)。
  - 周线级别买卖点可用较大仓位 (50-80%)。
  - 多级别共振（日线+周线同方向）时信号最强。

  ### 5. 输出要求
  - 明确说明当前处于：上涨趋势/下跌趋势/中枢震荡。
  - 指出是否存在背驰信号及背驰级别。
  - 给出当前买卖点类型（一买/二买/三买 等），若无则写"暂无明确买卖点"。
  - 止损设于前低（买入时）或前高（卖出时）。

  评分调整建议：
  - 底背驰 + 一买信号：`sentiment_score +15`
  - 二买/三买共振：`sentiment_score +10`
  - 中枢震荡无明确方向：`sentiment_score 维持基准`
  - 顶背驰 / 趋势向下：`sentiment_score -15`
```

## 参见

- [波浪理论 (wave_theory)](wave_theory.md) — 结构分析的互补框架
- [默认多头趋势 (bull_trend)](bull_trend.md) — 趋势确认的补充验证
- [箱体震荡 (box_oscillation)](box_oscillation.md) — 中枢震荡的实操方法
- [情绪周期 (emotion_cycle)](emotion_cycle.md) — 背驰与情绪极端的关联
