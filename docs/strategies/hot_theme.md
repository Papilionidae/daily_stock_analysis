# 热点题材 (Hot Theme)

## 基本信息

| 字段 | 值 |
|------|-----|
| 策略 ID | `hot_theme` |
| 名称 | 热点题材 |
| 分类 | 框架 (framework) |
| 优先级 | 35 |
| 默认激活 | ❌ |
| 关联理念 | 第 2 条（趋势交易）、第 3 条（筹码效率）、第 5 条（风险排查）、第 7 条（强势趋势股放宽） |
| 市场状态 | `sector_hot` |
| 别名 | 热点、题材、热点题材 |

## 核心逻辑

跟踪政策、产业和市场热点，判断题材强度、板块扩散和个股相对强弱。

### 判断链

1. **热点强度**
   - 调用 `get_sector_rankings` 判断相关板块是否在涨幅/成交额上处于前列
   - 观察热点是否从少数核心股扩散到板块内多只个股
   - 若只有单股异动、板块未共振，降低信号权重

2. **个股相关性**
   - 调用 `search_stock_news` 检查公司业务是否与热点直接相关
   - 区分：实质受益 / 间接受益 / 概念关联较弱
   - 概念关联弱但涨幅过大时，提示题材兑现风险

3. **相对强弱**
   - 调用 `get_realtime_quote` 与 `analyze_trend` 判断个股是否强于板块平均
   - 强势热点股通常表现为放量、换手活跃、回调不破关键均线

4. **节奏与风险**
   - 不在连续加速、高乖离率位置追涨
   - 若新闻集中在"已经大涨""资金追捧""龙虎榜游资博弈"，警惕短线情绪顶
   - 重大利空、监管问询、澄清公告可一票降低评级

### 热点阶段

需明确判断当前热点所处的阶段：

| 阶段 | 特征 | 策略 |
|------|------|------|
| 启动 | 消息催化，板块初涨 | 积极介入 |
| 扩散 | 热点向板块内扩散 | 追龙头或补涨 |
| 分化 | 真伪龙头分化 | 去弱留强 |
| 退潮 | 热点降温，资金流出 | 减仓/离场 |

### 评分调整

| 条件 | 调整 |
|------|------|
| 热点处于启动或扩散期，个股实质受益 | `sentiment_score +12` |
| 个股强于板块并有量能确认 | 额外 +6 |
| 热点进入分化或退潮 | `sentiment_score -8` |
| 仅概念蹭热点且乖离率过高 | `sentiment_score -12` |

## 技术指标与数据依赖

| 数据/工具 | 用途 |
|-----------|------|
| `get_sector_rankings` | 板块涨幅、成交额排名 |
| `search_stock_news` | 热点相关新闻、政策、公告 |
| `get_realtime_quote` | 个股量比、换手率 |
| `analyze_trend` | 趋势评分、均线位置 |

## 适用场景

- **适合**：政策驱动行情（如新能源、AI、国企改革）、产业催化行情
- **不适合**：无明确题材驱动、个股独立行情
- **市场阶段**：题材炒作活跃期、政策密集期

## 与上下游策略的关系

| 策略 | 关系 |
|------|------|
| `sector_rotation` | 互补：板块动量 + 题材热度双重验证 |
| `dragon_head` | 配合：确认题材强度后，用 `dragon_head` 锁定题材龙头 |
| `event_driven` | 延伸：题材中的具体催化事件分析 |
| `emotion_cycle` | 风险控制：热点退潮期可切换到情绪周期策略 |

## 在管道中的角色

- **specialist 模式**：当市场状态为 `sector_hot` 时自动选入
- **题材验证**：判断个股是"实质受益"还是"蹭概念"，后者需大幅减分
- **阶段输出**：必须指明热点阶段（启动/扩散/分化/退潮）

## YAML 配置原文

```yaml
# Hot Theme Strategy / 热点题材

name: hot_theme
display_name: 热点题材
description: 跟踪政策、产业和市场热点，判断题材强度、板块扩散和个股相对强弱。
category: framework
core_rules: [2, 3, 5, 7]
required_tools:
  - get_sector_rankings
  - search_stock_news
  - get_realtime_quote
  - analyze_trend
aliases: [热点, 题材, 热点题材]
default_priority: 35
market_regimes: [sector_hot]

instructions: |
  **热点题材策略（Hot Theme Strategy）**

  适用场景：
  - 市场出现明确政策、产业、技术路线或资金抱团热点。
  - 需要判断个股是否真正受益于热点，而不是单纯蹭概念。

  分析框架：

  1. **热点强度**
     - 使用 `get_sector_rankings` 判断相关板块是否在近期涨幅、成交额或人气上处于前列。
     - 观察热点是否从少数核心股扩散到板块内多只个股。
     - 若只有单股异动、板块未共振，降低信号权重。

  2. **个股相关性**
     - 使用 `search_stock_news` 检查公司业务、订单、产能、客户、公告是否与热点直接相关。
     - 区分"实质受益""间接受益""概念关联较弱"。
     - 概念关联弱但涨幅过大时，应提示题材兑现风险。

  3. **相对强弱**
     - 使用 `get_realtime_quote` 与 `analyze_trend` 判断个股涨幅、量比、换手率是否强于板块平均。
     - 强势热点股通常表现为放量、换手活跃、回调不破关键均线。

  4. **节奏与风险**
     - 不在连续加速、高乖离率位置追涨。
     - 若新闻集中在"已经大涨""资金追捧""龙虎榜游资博弈"，需要警惕短线情绪顶。
     - 重大利空、监管问询、澄清公告可一票降低评级。

  输出要求：
  - 明确当前热点阶段：启动 / 扩散 / 分化 / 退潮。
  - 说明个股与热点的实质相关性。
  - 给出可执行触发条件：回踩承接、放量突破、板块继续共振或退潮止损。

  评分调整建议：
  - 热点处于启动或扩散期，且个股实质受益：`sentiment_score +12`
  - 个股强于板块并有量能确认：额外 `+6`
  - 热点进入分化或退潮：`sentiment_score -8`
  - 仅概念蹭热点且乖离率过高：`sentiment_score -12`
```

## 参见

- [行业轮动 (sector_rotation)](sector_rotation.md) — 板块动量维度
- [龙头策略 (dragon_head)](dragon_head.md) — 题材龙头锁定
- [事件驱动 (event_driven)](event_driven.md) — 题材中的具体催化事件
- [情绪周期 (emotion_cycle)](emotion_cycle.md) — 题材退潮期的情绪判断
